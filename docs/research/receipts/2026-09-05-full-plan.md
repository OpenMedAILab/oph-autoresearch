# 完整计划执行回执（持续更新）

日期：2026-09-05。分支 `feat/mvp-research-workbench`，起始 HEAD `e4d3322f19b33c545819b4b5e15a50eaee4c5d33`。本回执记录本轮新增证据，不修改上一轮 `2026-09-05-restricted-lineage.md` 的历史结论。

用户授权继续全部计划、GPT-6 Astra 主控、GPT-5.6 Terra 实现、GPT-6 Astra 独立审查，完成后打包并推送。本地实现与打包结果见末尾最终交付记录；外部环境验收不由本机测试替代。范围见 `../full-plan-execution.md`。

## 已有独立审查与真实测试证据

| 范围 | 证据 | 限制 |
|---|---|---|
| 安装包收集器 | 独立 Astra 复核；3 项测试、11 个断言。仅收集显式目标、当前版本的唯一 NSIS 文件；拒绝路径 junction/symlink；新建输出目录，核验复制 SHA256 | 本轮新安装包已构建，精确 hash 见最终交付记录 |
| 可信审批签名 | 独立 Astra 复验 28 项测试、96 个断言；真实 HTTP 同签名重试 200/200；保留首次 verifiedAt，拒绝伪造 KeyObject | 测试公钥不代表外部医生身份已接通 |
| 合成训练模板 | 独立 Astra 复验 19 项测试、71 个断言；新增模板保留旧模板固定输入与历史 hash | train-only 均值拟合加固定 logistic scorer，不是训练图像分类器 |
| MLflow/DVC 只读收集器 | 独立 Astra 复核 3 项测试、19 个断言；真实 loopback HTTP/隔离 Git；继承 GIT_DIR 来源替换探针已拒绝；后续实际 worker 消费链已通过独立复核 | 无机构服务或数据下载验收 |

## 独立 HTTP 纵切片

新增 `scripts/research-evaluation-smoke.ts` 通过实际 HTTP 创建 Campaign、派发同一 Runner/Attempt、读取并核验产物。预期指标由独立手算提供，不调用实现 evaluator 生成 expected。验证 validation 阈值 0.8、test AUROC 1、敏感度 0.75、特异度 1、准确度 6/7、4 个 test 患者聚类；聚合报告不含 patientId。报告篡改和策略过期均被 409 拒绝，重复派发只有一个 Attempt。

- 固定 score source 模式：`.tmp/research-evaluation-smoke/run-bWyucq/receipt.json`，2867 bytes，SHA256 `591dcbbea26ed23e7295763531acf7ff9fed02a7fd806ee4a75c55acd0e17a7c`。
- train-only source 模式：`.tmp/research-evaluation-smoke/run-wvha7p/receipt.json`，3314 bytes，SHA256 `dc78adc6afc8018e0a96488d02a0f9033cc0714331d75cb1ada00a5e26eeb651`。训练均值 `2.220446049250313e-16`；2 个训练观察。参数篡改被 409 拒绝。
- 训练输入 hash：`sha256:b457b6ccd691f6c27c977c752a6af681e5e5aa798e8feed4a45fe6d338085c3c`；实际参数 hash：`sha256:c69f461efafd6b509204c037732de29a2ee2ea912380fbb1515c7926ae11a9de`。

两个 compiled 模式已在最终构建后通过，证据见末尾。上述局部测试不能代替最后全量 gate，也不证明事前临床审批或临床性能。

合成图像 source HTTP 纵切片：`.tmp/research-evaluation-smoke/run-UlUQjz/receipt.json`，1761 bytes，SHA256 `78b62aee17a9a13f1a92538159d52d209c0e5a5dc917bd94460f41f54683f8b2`。实际处理 11 张 8×8 固定合成灰度图像，train-only mean 0；validation 阈值由像素均值独立手算为 `logistic((172-128)/32) = 0.7981867777396212`。特征 hash、拟合参数与报告篡改均 409，重复派发只有一个 Attempt，策略更新后收据 409。图像模板不是临床图像分类器。

## 公开文献与受预算控制的模型审查

公开收集器真实访问 Crossref 与 NCBI 成功，来源及语义限制见 `../literature-source-audit-2026-09-05.md`。独立 Astra 对单体收集器未发现新缺陷，定向 loopback HTTP 3 项测试、30 个断言。接口只收 DOI/PMID，引用准备只认已核验收集器实例，返回元数据不冒充全文。

`scripts/research-review-smoke.ts --literature` 的实际 source HTTP 验证贯穿官方 Crossref → Campaign 引用落账 → EvidencePack → fresh Session → local SSE 请求；检查发给模型的真实请求包含公开引用、没有 Campaign/工作区私有 canary。使用临时测试签名公钥：无签名审批 403，同签名重试不产生新版本。两次模型审查各发一次请求，重放不重发；真实 wire `max_output_tokens: 1024`。一个请求提供用量，记配置价格 `0.000146`；另一个省略用量，`actualCost: null`。两条记录分别保留 `10.536864` 的配置价格预算预留，不把未知用量记零。该保守预留基于完整上下文及全部价档，不是供应商账单保证。

回执 `.tmp/research-review-smoke/run-OigbuM/receipt.json`，3424 bytes，SHA256 `94ba95cf022830f2ea3e4c2cbf91d45703ac4919c63a3ad6770cdad5d77e817a`。模型端是本地 SSE fixture，未调用付费模型；测试签名不是真实人类审批。compiled 模式已通过，预算集成独立审查问题已关闭。

Daemon 独立复现关闭两项缺陷：取消确认不再于子进程存活时标记终态；队列已满时排队 worker 的 650ms 启动次数由 7 降至 0。worker 先取得资源槽再计算，最终结果与取消仍等待实际退出。内存请求字段本身不代表 OS 硬限制，须按最终部署实现记录。

## 消费链与外部前置

内置 CLI 审核消费者：`--research-review-cli` 使实际审查 Session 在本项目的独立 CLI worker 中执行。只通过 stdin 传入锁定 EvidencePack、公共模型规格和临时 loopback 凭证；provider 凭证与原审批/请求预算裁决留在父进程。worker 使用新 scratch、最小环境和内存 Store，不接受 resume、不读取父工作区；结构化结果重新核验 pack hash/产物版本，最终仍写入原 ModelReview。报价 configHash 绑定 `builtin-cli` 或 `builtin-session`，UI 显示实际 backend。子进程 runId/conversationId 是执行来源标识，其临时会话不写入主会话库。

CLI source HTTP smoke `.tmp/research-review-smoke/run-G7OJxA/receipt.json` 的 SHA256 为 `d95f20a3a048d142801d4f6ba05ebe0e54c0eb2d8edb0a9fd2384ab5834e9c67`；独立 Astra 另跑 `run-JG5NDj` 同样通过。正式 CLI/审查回归 10 项测试、51 个断言通过：真实 worker 环境与 cwd 观测、tool read 后第二请求、第三请求阻断、并发唯一派发、无 scratch 会话污染、取消在异步 mkdir 窗口后不 spawn。Astra 报告的取消竞态已用原探针关闭。测试 provider 是本地 SSE，不是付费真实模型；该内置 CLI 不等于外部 Codex/Claude CLI 已获相同能力，也不声称 cwd/minenv 是 OS 强隔离。

2026-09-05 追加：模型审查独立 Astra 批次 17 项测试、68 个断言通过。原 Anthropic 探针从允许 1024、实际出站 16000 修复为两者均 1024；`hardOutputLimit` 只由研究请求 guard 设置，不改变普通请求的 thinking floor。并发相同 dispatch 只发一次真实 SSE 请求，已完成 review 在策略或生产 TaskRevision 更新后派生为 stale，保留历史执行与用量事实。fresh Evidence Session 不再把临时 scratch 注册成用户工作区。

Tracking 通过实际 worker → loopback MLflow/固定 Git DVC lock → 同一 Attempt/JobSpec/ArtifactVersion → receipt/EvidencePack。审批、Attempt、JobSpec 绑定 startup trackingPolicyHash；旧不含来源配置 hash 的批准在网络读取前拒绝。根代理最新测试 3 项、30 个断言通过，包括来源 503 后无已验证产物、错误正文未传播、重放不重查。HTTP 源码回执 `.tmp/research-evaluation-smoke/run-qUFvqd/receipt.json` SHA256 `4315351bbf1c2a13c789266dab8cf07f0213589254319d303111b17773611859`；compiled 模式已通过，见最终交付记录。

Pattern 的 `allowPublicMetadata:false` 曾未被文献 API 消费；修复后独立原探针 HTTP403 且 upstreamCalls=0。新 `auto` 模式只在三个已准入、自包含的固定合成模板中按注册顺序选择首个达标项；不是跨真实数据集模型排名。筛选失败写入同一 Pattern 事件与投影，任务列表为空、拒绝执行/发布、重新规划保留历史评测。这一追加批次独立审查通过。

Daemon/unknown reconciliation、Evidence Session、模型预算及其 HTTP 消费、六阶段 UI 与其余 P1/P2 由实施主控整合，独立 Astra 复核稳定批次。只有实际接线及验证结果进入完成记录。

尚未获得专用非患者 SSH 环境与可信人类身份系统配置；未连接真实患者数据、未安装未审核第三方项目。继续完成本地合同与多进程验证，外部身份、服务器 ACL、机构网络及真实远端验收须按真实证据单独报告。

## 最终交付记录

2026-09-05 本地实现、独立审查及打包完成。最终 `bun run gate` 退出 0：2380 pass、1 平台 skip、0 fail，11899 assertions / 184 files；TypeScript（含 web）、Biome、Rust check 全通过。日志 `.tmp/pro-review-2026-09-05/full-plan/final-gate-4.log`。备份最后修复只涉及不进入安装包的演练脚本；应用运行时代码与已验收安装包一致。

`bun run build` 与 `CI=false bun run tauri:build` 均退出 0，日志分别为同目录 `final-build.log`、`final-tauri-build.log`。实际构建目标 `x86_64-pc-windows-gnu`，版本 0.1.8。GNU linker 有 `.rsrc merge failure: multiple non-default manifests` 警告；构建及 NSIS 打包成功，静态读取 app 内嵌 manifest 含 `longPathAware=true` 与 `asInvoker`。未运行安装程序，不声称安装后桌面人工验收。

安装包为 `.tmp/installer/v0.1.8-x86_64-pc-windows-gnu-zCKJpP/oph-autoresearch_0.1.8_x64-setup.exe`，32,879,171 bytes，SHA256 `531d056ef8f6c30ae3bf9d8e27627f62aa47d7c21218df368cea09cd91bc574f`。同目录 `package.json` 与 `SHA256SUMS.txt` 由收集器生成并独立复核；没有用旧缓存包替代。编译 CLI SHA256 `b69a0db0dfa8a52b888f2860309d5a49b38ffd06dd18063a4cedd3294ad306ef`，全部烟测使用与最终 sidecar 字节一致的副本。

| compiled 验证 | 回执 / 日志（本地 .tmp） | SHA256 |
|---|---|---|
| summary | `pro-review-2026-09-05/full-plan/compiled-summary.log` | `ca141318c8c80596cdffcdd8b1fa84b402b7ded0a748a3ef3181e9683ea151f3` |
| score | `research-evaluation-smoke/run-PjMHvM/receipt.json` | `33e5fb826ec46929c100843e5c9edd567a696dc80e0a02fa3639e69ecde9af9c` |
| training + daemon | `research-evaluation-smoke/run-Ua3wVn/receipt.json` | `3f1dc54ece4ec983656858c6859e2bf99de192dd084d446456b166c6e8f5f962` |
| retinal + tracking | `research-evaluation-smoke/run-w0dD2m/receipt.json` | `1e25e51686db0dbb9907f90024ff166862cabdea0016c73b0e7c2d13f3611011` |
| CLI review | `research-review-smoke/run-H7SZfI/receipt.json` | `72e42651f8f0791bd658e5d6558aac32da9793dc10a7e7fb9b21b5f489c321fa` |
| 官方 Crossref + Session review | `research-review-smoke/run-59kMAK/receipt.json` | `2ebd5208a659b1b736b57a2a211144aa8b3319afec26df3948c54ef4ebbccf18` |
| 常驻 authority | `pro-review-2026-09-05/full-plan/compiled-authority-receipt.json` | `ed1b6246f8f39e6767ecf55fe666162336d380e5f31fac12ee89c4af9634258f` |

常驻 authority 使用实际编译 daemon 与 worker，在本机回环提交 100 次同键请求，重启后第 101 次仍返回原已完成回执，总作业数 1。不是实际 SSH 网络验收。模型请求使用本地 SSE fixture；官方 Crossref 为真实公开网络请求。

备份修复后的持久演练回执 `.tmp/pro-review-2026-09-05/full-plan/backup-drill-verified-receipt.json` SHA256 `3358080cbe7ccb282a8fea43ac87da13701c1fc32dc5edbde3594cd72597605f`；源为实际合成 tracking HTTP 账本。恢复库执行 `PRAGMA integrity_check` 与 `foreign_key_check`，比较 Campaign/event/审批/Attempt/产物 hash。备份 DB SHA256 `25f99ab906d3abbf47774d72b65662e11f44436ed535e2403cfe9a1b8c99a45c`；产物 SHA256 `2818c106da5d06d456f1f4ed1b86b8bd313643cc2b2bb7b7f9e71dc500614036`。独立 Astra 原双真实 Runner 产物探针与正式备份回归共 3 pass、21 assertions，关闭第二产物重复 mkdir 的 EEXIST。演练不删除或覆盖已有路径，恢复库保留 immutable 原 file URI，不证明 URI 重定位后的应用恢复。

最终独审没有剩余已确认代码缺陷。已关闭远端恶意回执校验前落盘、恢复查询替代 authority、CLI 取消竞态、Pattern 状态与权限消费、模型输出上限、备份多产物等发现。结论仅适用于被验证代码与环境，不保证不存在其他缺陷。

保留检查为 124/124 原 dirty 文件存在，111 字节未变，13 为必要整合修改，0 缺失；详见本地 `preservation-final.json`。既有 UI/PDF/usage/runtime/team/tools/SSH 成套源码与锁文件一并交付，个人 `.agents/memory`、`.tmp`、生成二进制、临时密钥和数据库不提交。源代码推送目标仅为 `OpenMedAILab/oph-autoresearch` 的 `feat/mvp-research-workbench`；实际提交 SHA 和 push 后远端核对结果写入安装包旁 `delivery.json`，避免在提交内容中自引用 commit hash。不合并 master、不打 tag、不发布 Release。

外部尚缺专用非患者 SSH 主机、机构 ACL/防火墙/服务账号与可信人类身份配置。未连接患者数据，未执行被拒绝的第三方 Skill，未宣称临床准确率、实际医生审批、实机 SSH 或机构上线验收完成。
