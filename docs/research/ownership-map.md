# 本轮文件责任与整合

2026-09-05，基线见 `implementation-baseline.md`。

| 执行者 | 责任 |
|---|---|
| GPT-6 Astra 实现主控 | 共享 core 合同、包导出、schema/迁移编号、agent/runtime 热文件、server/CLI/UI 接线；整合 Terra 交付、修复审查发现、最终 gate/build/真实编译 HTTP 验证及回执 |
| GPT-5.6 Terra 子代理 | 按主控逐批指派的独立模块与定向回归；共享文件仅在主控明确交接后修改，不与主控同时编辑同一文件 |
| GPT-6 Astra 独立审查 | 只读核对实现、独立复现并向主控报告具体发现、复验修复；不直接覆盖实现文件 |
| 当前任务协调主控 | 固定上游与第三方来源取证、来源哈希核验、审计文档、审查协调与结果核对 |

所有执行者共享同一工作目录。用户原有改动和前两批实现属于本轮输入，不以“未提交”为理由删除。禁止重置、清理或用上游文件覆盖当前热文件。构建和测试只使用仓库约定的 `.tmp` 隔离产物。

开发代理模型分工不写死为软件科研角色的模型选择。审查通过也不等于用户亲测通过。
